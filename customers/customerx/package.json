{
  "name": "$NAME",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build"
  },
  "version": "$VERSION",
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.10"
  },
  "dependencies": {
    "@adhese/sdk": "^0.9.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "url": "git+https://github.com/$REPOSITORY.git"
  }
}
