// Multiple customers need this resource (with some minor customization) thus the `greet` function is imported from `common`.

import { greet } from '../../../common/src/example/hello';

greet("customerx");
